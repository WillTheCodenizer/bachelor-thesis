PROMPT:

You are helping me write the Methodology section of my Bachelor's thesis in astrophysics/cosmology. You have access to my code repository, the plots generated by it, and my LaTeX source for the theoretical background chapter (provided below). Your task is to write the methodology section in LaTeX, matching my existing writing style precisely.

STYLE REQUIREMENTS:

Match the style of my existing LaTeX code: formal academic English, concise but thorough, with inline math using \begin{equation*} or \begin{equation} environments
Every non-trivial claim, formula, parameter value, and methodological choice must be backed by a \cite{...} reference — especially:
Where specific numerical values come from (e.g. cosmological parameters, alpha values, bias parameters)
Which Python packages/modules were used and for what purpose (cite the package papers)
Which approximations are used and their validity conditions
Which published redshift distribution or bias model I adopt
Figures produced by my code should be referenced with \autoref{fig:...} and have captions that describe what is shown and what can be concluded
Equations should be numbered when referenced later, otherwise use equation*
Section and subsection structure should follow the logical flow of the analysis pipeline
Depth and thoroughness should match this example from another thesis (provided below as a reference)

REFERENCE STYLE EXAMPLE (match this level of detail):

Assume a normalised redshift distribution of the following form (Reischke, Hagstotz, and Lilow 2021):
n(z) ∝ z² exp(−αz).
In this thesis, α = 3.5 is assumed, corresponding to a shallow survey (see Reischke, Hagstotz, and Lilow 2021). The associated source distance distribution is then given by n(χ) = n(z) · dz/dχ with the Jacobian dz/dχ = H(z)/(H₀ · h · χ_H). A plot of the redshift distribution is shown in Figure 7. The averaged weighting function is numerically calculated in Python using the trapezoidal rule for integration as given in equation 15, a plot shown in Figure 8.

Note how this example:

States the functional form of the distribution with a citation
Justifies the choice of parameter value with a citation
Explains the mathematical connection to adjacent quantities
References the numerical method used
Points to the corresponding figure

CONTENT TO COVER (infer details from my code and plots):

Please write a methodology section covering the following pipeline steps. For each step, look at the relevant code and plots and describe:

Cosmological Model and Fiducial Parameters — Which ΛCDM parameters are assumed, their values, and where these values come from. Present them in a LaTeX table (\begin{table}...\end{table}).
FRB Redshift Distribution — The functional form of n(z) adopted, the motivation and source for this choice, the value of any free parameters (e.g. α), and the Jacobian transformation to n(χ). Reference the corresponding plot.
Galaxy Redshift Distribution (KiDS) — How the photometric redshift distribution of the KiDS galaxy sample is obtained or modelled, any cuts applied, and the resulting n(z). Reference the plot.
Window / Weighting Functions — How the radial kernel functions W^A(χ) and W^B(χ) are computed for FRBs and galaxies respectively (refer to the theoretical definitions already established in my background chapter). Describe the numerical integration method used.
Angular Power Spectrum Computation — Which code/package is used to compute C_ℓ (e.g. CCL, TreeCorr, or a custom implementation). State which approximations are applied (e.g. Limber approximation, nonlinear vs linear power spectrum). Cite the relevant packages and approximation papers.
FRB Host-Population Bias Model — Describe the redshift-dependent bias b(z) assigned to each progenitor class (magnetar / delayed channel). State the parametrisation, the specific values used, and their source. Reference the plot of b(z) if present.
Cross-Correlation Estimator — How the angular cross-power spectrum between FRBs and galaxies is estimated in practice (e.g. pseudo-C_ℓ with mask, NaMaster/pymaster, etc.). How the mask is applied and how mode coupling is corrected.
Covariance Matrix and Error Estimation — How uncertainties on C_ℓ are estimated (analytical Gaussian covariance, jackknife, simulations). State the relevant formula and its source.
Signal-to-Noise and Fisher Forecast (if applicable) — If my code contains a Fisher matrix or SNR calculation, describe the setup: which parameters are varied, which fiducial values are used, how the Fisher matrix is computed.

INPUTS YOU HAVE ACCESS TO:

My full code repository (examine the actual functions, parameter values, and logic used)
All plots generated by the code (describe what each shows and reference them)
My theoretical background LaTeX chapter (provided below — use \autoref{} to cross-reference equations and sections already defined there, e.g. the Limber approximation, the window function formalism)

THEORETICAL BACKGROUND LATEX (for cross-referencing):

[PASTE YOUR FULL THEORETICAL BACKGROUND LATEX HERE]

OUTPUT FORMAT:

Valid LaTeX, ready to paste into my thesis
Use \section{}, \subsection{} structure
Use \label{sec:...} for all sections
Use \autoref{} for cross-references to figures, equations, and sections
Use \cite{} placeholders like \cite{AuthorYear} wherever a source is needed — I will fill in the BibTeX keys afterward if you don't know them exactly, but make your best effort based on the context (e.g. packages like astropy, numpy, scipy, pymaster/NaMaster, CCL should be cited)
Include \begin{table} for the cosmological parameter table
Do not omit figures — reference every relevant plot with a \begin{figure} environment including a descriptive caption